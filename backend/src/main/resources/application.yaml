spring:
  application:
    name: backend
  profiles:
    default: h2
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: true

kulcha:
  cors:
    allowed-origins: ${KULCHA_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175,http://127.0.0.1:5173,http://127.0.0.1:5174,http://127.0.0.1:5175}
